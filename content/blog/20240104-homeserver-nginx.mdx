---
title: 실제로 맥북의 8080포트에 웹서버 띄우기
date: '2024-01-04'
tags:
  - homeserver
  - nginx
  - reverse-proxy
  - load-balancer
category: dev
summary: 'Nginx 설치부터 웹서버, 리버스 프록시, 로드밸런서 구성까지의 실습 기록'
thumbnail: ''
published: true
---

현재는 외부에서 접속을해도 맥북의 `8080` 포트에는 아무것도 존재하지 않기 때문에, 접근 거부?가 뜬다.

그렇다면 실제로 접근했을때 무언가를 보여주기 위해, **Nginx**라는 웹서버를 설치한 다음 `8080` 포트에 특정 문구를 띄워주려고 한다.

homebrew를 사용해서 nginx를 설치해준다. 명령어는 `brew install nginx` 이다.

설치가 완료되면, `brew services start nginx`로 nginx를 실행 시킬 수 있다.

nginx를 설치만해도, 기본적으로 `8080` 포트에 nginx welcome 이라는 문구를 띄워준다. 추후 `8080` 포트를 `80` 포트로 변경하면, 뒤에 포트번호를 명시하지 않고 접근시에도 (http 기본 `80` 포트) welcome nginx를 볼 수있다.

즉 nginx를 설치하여 맥북의 `8080` 포트로 접근하면 welcome nginx를 보여줄 수 있게 만들었다.

위의 과정을 통하면, 외부 IP를 통해 내 맥북의 `8080` 포트에 띄워진 nginx 웹서버가 서빙해주는 텍스트를 볼 수 있다.

![homeserver-nginx-01.png](/blog/images/20240104-homeserver-nginx.png)

- 이건 같은 공유기를 사용하는 아이패드에서 맥북 :`8080` 포트에 접근했을때의 화면이다.

![homeserver-nginx-02.png](/blog/images/20240104-homeserver-nginx-01.png)

- 이것은 공유기 외부에서, 맥북의 `8080` 포트에 접근했을때 나오는 화면이다.

다음시간에는 맥북에 설치된 nginx의 config와 맥북에 어디 설치되었는지 nginx 자체를 분석해보려고 한다.

---

## 맥북에 Nginx 웹서버를 설치한다는것은?

어제 맥북에 nginx 설치를 하고, `8080` 포트에서 nginx가 실행하도록 명령어를 작성하였다.

현재 사용하고 있는 맥북에서, `localhost:8080`으로 접속했을때 nginx가 떠있는 것을 볼 수 있다. (정확히는 `location`에서 가르키는 html 파일이 뜨고 있는 것)

오늘은 `nginx`를 조금 더 자세하게 살펴 보려고 한다.

![맥북의 8080포트에서 nginx가 돌아가고 있는 것을 확인할 수 있다.](/blog/images/20240104-homeserver-nginx-02.png)

### 일단 맥북의 어디에 nginx가 설치되었는지 확인해보자!

- 일단 `cd ..` 명령어로 맥북 디렉토리 최상단으로 벗어난 다음, `/usr/local/etc/nginx/nginx.conf` 에 있는 `nginx.conf` 이름으로 되있는 nginx config 파일을 열어보자.
- `vim`을 사용해서 해당 파일을 열 수 있다. - `vim nginx.conf`
- 대략적으로 이러한 내용이 적혀져 있다. 겁나 길다…

> [!📎] 원본 Notion 첨부파일 (마이그레이션 미지원)

- 일단 vim 편집기로 수정을 해볼건데, `GPT`가 알려준 vim 수정모드 다음 단축키로 하면 된다고 한다.
- **`i`**: 편집 모드로 전환
- **`Esc`**: 명령 모드로 전환
- **`:wq`**: 저장하고 종료
- **`:q!`**: 저장하지 않고 종료

- 일단 그나마 제일 알 것 같은 sever 블록을 살펴보자

```shell
server {
        listen       8080;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   html;
            index  index.html index.htm;
        }
```

- 현재 맥북의 `8080` 포트에서 듣고 있다.
- `8080` 포트를 `80` 포트로 변경한 후 재시작 해보자
- nginx 재시작 명령어

```shell
brew services restart nginx
```

- 이전 `8080` 포트로 접근 했을때는 다음과 같은 화면을 볼 수 있다.

![homeserver-nginx-04.png](/blog/images/20240104-homeserver-nginx-03.png)

- 이번에는 좀 전에 `80` 포트로 변경했으니, `80` 포트로 접속해보자! 하지만 `80` 포트는 well-known포트로 http 통신할때 포트를 생략할 수 있다!

- `nginx.conf`에 있는 location에 있는 html은 상대경로이고, 실제 html이 가르키는 파일을 찾아보려고 한다.
- `/usr/local/Cellar/nginx/1.25.3`에 진입하여 `ls -l` 명령어로 어떤 파일들이 있는지 확인해보자 (해당 경로에 진짜 nginx파일이 존재한다.)

```shell
-rw-r--r--  1 kyusikko  admin  325032 10 24 22:46 CHANGES
-rw-r--r--  1 kyusikko  admin    1443  1  3 23:24 INSTALL_RECEIPT.json
-rw-r--r--  1 kyusikko  admin    1397 10 24 22:46 LICENSE
-rw-r--r--  1 kyusikko  admin      49 10 24 22:46 README
drwxr-xr-x  3 kyusikko  admin      96 10 24 22:46 bin
-rw-r--r--  1 kyusikko  admin     679  1  3 23:24 homebrew.mxcl.nginx.plist
-rw-r--r--  1 kyusikko  admin     196  1  3 23:24 homebrew.nginx.service
lrwxr-xr-x  1 kyusikko  admin      16  1  3 23:24 html -> ../../../var/www
drwxr-xr-x  4 kyusikko  admin     128 10 24 22:46 share
```

- `html -> ../../../var/www` 이렇게 표기 되어있는데, `->` 는 바로가기라고 이해하면 된다. 즉 경로를 풀어보면 `/usr/local/Cellar/nginx/1.25.3/../../../var/www`의 경로에 실제 보여주는 파일들이 존재하는 것을 알 수 있다.
- 그렇다면 `/usr/local/var/www`로 진입해 본다.

```shell
-rw-r--r--  1 kyusikko  admin  497 10 24 22:46 50x.html
-rw-r--r--  1 kyusikko  admin  615 10 24 22:46 index.html
```

- 여기에 위치한 `index.html`이 `nginx`를 켰을때 보여주는 실제 html이다!
- vim 명령어로 `index.html` 파일을 열어보자.

```shell
vim index.html
```

- 다음으로 해보려는 것은 위의 경로에 있는 html이 아닌 내가 직접 `html`을 만들어서 해당 html이 위치하는 경로를 nginx의 location에 지정해 줄 것이다.
- 평소에 하는 것처럼 아무때나 폴더를 만들고, 그 절대경로를 `nginx.conf`의 location 블록에 지정해 보자!
- 나는 `Programming_ko` 디렉토리에, `kyusik_server`이라는 디렉토리를 만들어 그 안에 `index.html`파일을 만들고 해당 경로를 찾는다.
- 팁은 터미널에서 `/Programming_ko/kyusik_server`에 들어가서 `pwd`라는 명령어를 입력하면, 해당 디렉토리의 절대경로가 나온다.

![homeserver-nginx-05.png](/blog/images/20240104-homeserver-nginx-04.png)

- 다음 경로를 복사해서, 아까 `nginx.conf` 파일의 location 블록의 root에 붙여넣기 해준다.

```shell
server {
        listen       80;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   /Users/kyusikko/Programming_ko/kyusik_server;
            index  index.html index.htm;
        }
```

- 여기서 궁금했던건 location 블록의 index인데 이것을 gpt한테 물어봤다.
- 즉, 클라이언트가 해당 디렉터리(`/Users/kyusikko/Programming_ko/kyusik_server`)에 접근했을 때 `Nginx`는 설정된 인덱스 파일을 찾아 제공하게 됩니다.
- 여기서 설정된 `index.html index.htm;`는 Nginx가 해당 디렉터리에서 먼저 `index.html` 파일을 찾고, 없으면 `index.htm` 파일을 찾아 제공하라는 의미이다. 순서대로 파일을 찾기 때문에 먼저 나오는 파일이 존재하면 그 파일이 제공됩니다.
- 위의 코드를 풀어 설명한다면, 클라이언트가 `http://localhost/`에 접근했을 때, Nginx는 설정된 디렉터리인 `/Users/kyusikko/Programming_ko/kyusik_server`에서 `index.html` 파일을 찾아 제공하려고 시도합니다. 파일이 존재하지 않으면 `index.htm` 파일을 찾게 됩니다.

> [!💡]
> 웹 서버가 기본적으로 제공하는 인덱스 파일을 설정하는 것이며, 클라이언트가 **특정 디렉터리**에 접근했을 때 서버가 어떤 파일을 우선적으로 제공할지를 결정합니다.

- `localhost:80`(`80` 포트는 생략)으로 접근했을때,

![우왓…내 컴퓨터에 있는 index.html을 보여주었다!](/blog/images/20240104-homeserver-nginx-05.png)

- 내 컴퓨터에 있는 html을 서빙받을 수 있다!!
- nginx `80` 포트를 외부 IP에 포트포워딩 시키면 외부의 클라이언트에서 해당 html을 서빙 받을 수 있다.

- nginx 시작 명령어

```shell
brew services start nginx
```

- nginx 종료 명령어

```shell
brew services stop nginx
```

- nginx 재시작 명령어

```shell
brew services restart nginx
```

> [!💡]
> 여기까지의 과정을 그림으로 표현해보자!

![homeserver-nginx-07.png](/blog/images/20240104-homeserver-nginx-06.png)

- 다음 해볼 내용
- nginx redirects (`proxy_pass`)

---

## 맥북의 호스트파일을 알아본다

컴퓨터 네트워크에서 호스트 이름을 IP 주소로 매핑하는데 사용하는 **로컬 운영체제 파일**이다. (생활코딩님의 DNS 강의를 참고하면 조금 더 상세하게 알 수 있다.)

해당 파일의 위치는 맥 기준으로 `/etc/hosts`에 위치하고 있다. 이제 부터 hosts 파일을 수정해 볼텐데, 항상 오리지널 파일을 수정하기 위하기전에 백업 파일을 생성하는게 좋다. `sudo`라는 키워드를 앞에 붙이지 않으면, permission denied가 뜨기 때문에 `sudo` 키워드를 붙여준다. 아래의 명령어는 `/etc/hosts` 경로에 있는 파일을 `/etc/hosts.bak`에 복사하는 것이다. 해당 경로에 파일이 없다면, 복사하여 파일을 새로 생성한다. 이 명령어를 수행하면, 같은 디렉토리내에 `hosts.bak` 파일이 생성이 된다. 추후 기존 hosts파일이 잘못되었을때, `hosts.bak` 파일로 다시 처음부터 셋팅을 할 수 있다!

```shell
sudo cp /etc/hosts /etc/hosts.bak
```

vim 편집기를 사용해서, hosts파일을 수정해보려고 한다. 추가로 수정할때도 `sudo`를 붙이지 않으면 수정이 불가하니 다음과 같이 명령어를 작성한다. `sudo vim hosts`

---

## 인프라 구성도를 그리고, 그것에 맞게 웹서버와 WAS를 셋팅한다.

![homeserver-nginx-08.png](/blog/images/20240104-homeserver-nginx-07.png)

위의 인프라 구성도를 그려보았다. 우측에 보이는 가장 큰 박스가 현재 사용하고 있는 `홈 서버 맥북`이다. (홈 서버 맥북은 항상 켜져있는 상태여야한다.)

현재 구성된것은

1. `8080` 포트에 띄워져있는 nginx 웹서버 : 정적파일을 서빙한다. (html파일을 서빙함)
2. `9000` 포트에 띄워져있는 nginx 웹서버 : **리버스 프록시** 역할을 한다.
3. `5000` 포트에 띄워져있는 WAS : `5000` 포트로 들어오는 응답을 받아 "hello"라는 메시지를 응답으로 돌려주는 서버이다.

위와 같이 Nginx를 2개로 분리한 이유는 만약 `8080` nginx를 하나라고 가정했을때, 외부 클라이언트에서 `8080` 포트로 들어오는 요청을 받았을때 정적파일에 WAS로 요청하는 코드 (`fetch('서버 url')`)이 있다면, WAS 요청 url이 브라우저에 그대로 노출이 되기때문에, 이를 방지하기 위하여, `9000` 포트의 nginx를 추가로 둔 것이다.

이렇게 한다면 바로 WAS의 url을 찌르는게 아닌 중간의 `9000` 포트에서 `5000` 포트로 중계를 해주기 때문에, 서버 url이 직접적으로 노출되지 않는다는 장점이 있다.

이것을 하기 위해서 `nginx.conf` 파일에 들어가, server 블록을 하나 더 추가한다.

```shell
# /usr/local/etc/nginx/nginx.conf

# 정적 파일을 서빙해 주는 nginx 웹서버
server {
        listen       8080;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   /Users/kyusikko/Programming_ko/kyusik_server;
            index  index.html index.htm;
}

### 위에 내용은 그대로

# reverse-proxy 역할을 하는 nginx 웹서버
server {
     listen 9000;
     server_name localhost;

     # proxy_pass로 프록시 설정을 해줄 수 있다.
     proxy_pass http://127.0.0.1:5000;
}
```

여기서 중요한 것은 **`proxy_pass`**를 통해서 `5000` 포트에 Listen을 하고 있는 WAS 서버로 요청을 중계(proxy)해준다는 것이다. 풀어서 설명하면

> [!🧠]
> 외부에서 `localhost:9000`으로 요청이 들어온다면, `http://127.0.0.1:5000` 주소로 요청을 보내버린다.

`conf` 파일을 수정했기 때문에 `brew services restart nginx` 명령어를 쳐서 nginx를 다시 실행시켜 준다.

그리고 express로 만든 서버(`5000` 포트에서 돌아가는)를 켜준다!

홈서버 맥북에서 `localhost:8080`으로 접근을 하게 되면

![homeserver-nginx-09.png](/blog/images/20240104-homeserver-nginx-08.png)

다음과 같이 정적파일을 받을 수 있고, hello라는 문구는 서버에서 받아서 화면에 그려준 결과이다!! 기대한대로 해당 정적파일에서 요청할때 WAS : `localhost:5000`으로 바로 요청하는것이 아닌 reverse-proxy역할을 하고 있는 `localhost:9000` nginx로 요청을 보내게 된다.

클라이언트 => `localhost:8080` => 정적 파일 서빙받음 => `localhost:9000/start` => `localhost:5000/start`
이런식으로 진행이되서 `hello`라는 응답을 받을 수 있다.

> [!🧠]
> 여기서 깨달은 것은 nginx를 2개 열어둘 수 있다는 것!, 그리고 reverse-proxy를 해서 서버 주소를 직접 노출하는 것을 방지하는 것이다.

### 하지만, 잘못된 점이 있었다.

지금까지는 홈서버 맥북인 로컬에서만 테스트를 진행했는데, 외부에서 만약 이 홈서버 맥북 IP:`8080` (`192.168.0.11:8080`) 로 접근을 해서 정적파일을 받았다.

정적파일까지는 잘 받아진다. 하지만 WAS서버로 요청이 `fail`한다. 그래서 hello 문구가 안그려진다. 왜그랬을까?…..

정답은 `nginx.conf`에 있는 `9000` 포트에 열어둔 `server_name`이 문제였다. 현재는 `server_name`을 `localhost`라고 작성해 놓았는데, 생각해보면 외부에서 해당 정적파일을 받았을때, `localhost:9000`으로 요청을 하게 되면, 해당 컴퓨터의 `localhost:9000`으로 요청을 하게 되는 것이다. 내 컴퓨터에 `9000` 포트에는 아무것도 열려있지 않을 것이기 때문이다.

즉 `server_name`을 `localhost`로 작성하는 것이아닌, 홈서버 맥북의 내부 IP를 적어줘야한다는 것이다.

```shell
### 위에 내용은 그대로

# reverse-proxy 역할을 하는 nginx 웹서버
server {
     listen 9000;
 # 여기에 홈서버 맥북의 내부 IP를 적어줘야한다는 것이다.
     server_name 192.168.0.11;

     # proxy_pass로 프록시 설정을 해줄 수 있다.
     proxy_pass http://127.0.0.1:5000;
}
```

이렇게 적어주면, 외부 컴퓨터에서 정적 파일을 받고, 그 정적 파일에서 `http://192.168.0.11:9000/start`로 요청을 하게되며, 홈서버 맥북의 `9000` 포트에 열려있는 nginx를 찌르게 되고, reverse_proxy기능으로 WAS에 요청이 가게된다. 이렇게 해야 외부 컴퓨터에서도 api 요청을 받을 수 있게 된다.

![homeserver-nginx-10.png](/blog/images/20240104-homeserver-nginx-09.png)

위의 사진은 홈서버 맥북이 아닌 나의 메인 맥북 16인치 화면이다. 주소 창을 보면 `192.168.0.11:8080`에 접속하여 해당 정적파일을 받고, 정적파일이 실행되며 `http://192.168.0.11:9000/start`로 api 요청을 보낸것을 알 수 있다.

그렇다면 홈 서버 맥북의 `express server`에 요청이 들어왔는지 확인을 해보자!

![homeserver-nginx-11.png](/blog/images/20240104-homeserver-nginx-10.png)

오옷! 신기하지않은가? 홈 서버 맥북의 express 서버에서 local이 아닌 외부의 요청을 받은 것을 확인 할 수 있다.

아이폰에서 `192.168.0.11:8080`으로 접속을 하게 되면, 16인치 맥북과 똑같은 정적파일을 받고, api 호출까지 하여 `hello` 문구를 찍는 것을 확인해 볼 수 있다.

![homeserver-nginx-12.png](/blog/images/20240104-homeserver-nginx-11.png)

### 오늘 배운것

1. nginx `reverse-proxy` 기능 => 클라이언트에 서버 주소를 직접적으로 노출 하지 않을 수 있다.
2. nginx의 역할을 2개로 분리
   1. 정적파일 서빙
   2. reverse-proxy
3. 외부에서 홈서버로 진입할때, WAS로 proxy시키려면 `server_name`에 해당 홈서버의 IP를 적어준다.
4. WAS 생성 => express

### 아쉬운점

현재는 정적파일을 수정하려면, 홈 서버에서 html 수정을 해야 하는데, 외부 컴퓨터 (맥북 16인치)에서 해당 정적파일을 수정하고, 뭔가 깃헙 같은데 올리면, 자동으로 홈 서버의 정적파일이 변경된다면 정말 편할 것 같다. 이게 아마 자동화 배포가 아닐까 생각이 든다.

그나마 위 방법을 사용하지 않고 외부에서 정적파일을 수정하여 최신화 시킬 수 있는 방법은 외부 컴퓨터에 똑같은 정적파일 디렉토리를 두고, 수정을 한다음 airdrop으로 홈서버에 해당 디렉토리를 넘겨, 홈서버에서 해당 경로의 정적파일을 교체하는 방법 정도는 될 수 있겠다. 하지만 불편할 것 같다.

> [!📎] 원본 Notion 첨부파일 (마이그레이션 미지원)

---

## 로드 밸런서로 WAS 서버 2개로 서버 부하 줄이기

홈서버 맥북 말고 만약에 외부 서버가 또 있다면??? 어떻게 구성하면 좋을까? 예를들어 크롤링서버같은 것을 둔다면 어떻게 구성을 해야하는 걸까?

오늘 아침에 빠르게 **로드 밸런서**로 서버 1과 서버 2 폴더를 각각 생성하고, server 1에서는 "hello server 1", server 2에서는 "hello server 2" 이런 메시지를 클라이언트에 내려준다. 클라이언트에서는 요청을 server1로 보낼지 server2로 보낼지에는 관심이 없다. 단지 nginx로 요청을 보내면, nginx에서 알아서 server1, server2로 중계를 해준다. (이 중계 조건은 다양한 알고리즘이 존재하고, 개발자가 원하는 중계 방식으로 설정하면 된다.)

로드 밸런서의 역할을 하기 위해서 서버는 다수를 띄울 수있지만, 각각의 서버가 내려주는 응답값은 똑같아야한다. 왜냐하면 단지 한곳의 서버에 요청이 부담되는 것을 막는거이기 때문에, 모든 서버에서는 동일한 값을 내려줘야 클라이언트는 항상 같은 응답을 받아 화면에 그릴 수 있다.

그렇다면 이제 nginx에 로드 밸런서를 어떻게 설정하는지 알아보자! 여느때와 다름없이 `nginx.conf` 파일에 들어간다. `9000` 포트로 기존에는 하나의 서버 `192.168.0.11:5000` 포트로만 `proxy_pass`를 해주고 있었다. 하지만 다음과 같이 코드를 변경한다.

```shell
upstream backend{
   random;
   server 192.168.0.11:3000 fail_timeout=3;
   server 192.168.0.11:5000;
}

# reverse-proxy 역할을 하는 nginx 웹서버
server {
     listen 9000;
 # 여기에 홈서버 맥북의 내부 IP를 적어줘야한다는 것이다.
     server_name 192.168.0.11;

     # upstream에 적어준 변수명을 적어준다.
     proxy_pass http://backend;
}
```

- 중요한 것은 **upstream** 블럭은 http 블럭안에 적어줘야한다는것. 그 안에 위치하지 않으면, `nginx -t`로 문법검사를 했을때 에러가 발생한다.
- upstream에 중계할 서버들을 나열해주면된다. `random` 키워드는 서버 분산을 랜덤으로 해주겠다는 이야기이고, 다양한 옵션들을 통해 중계 방식을 정할 수 있다. 그리고 `fail_timeout=3`; 이라는 키워드는 해당 서버가 3초이내 응답이 없다면 다른 서버로 요청을 보내는 방식이다.
- `proxy_pass`에는 `upstream`에 작성했던 변수?를 적어주면 로드밸런서가 형성이 된다.
- 중계 옵션은 해당 블로그 참고

> [!📎] 원본 Notion 첨부파일 (마이그레이션 미지원)

직접 한번 테스트를 해보자!

> [!📎] 원본 Notion 첨부파일 (마이그레이션 미지원)

새로고침 할때마다, server 1으로 요청을 보내거나, server 2로 요청을 보낸다. 클라이언트 입장에서는 server1, server2 어떤 서버로 보낼지 모른다.

![hello server 2 : 서버 2로 요청을 보냈다.](/blog/images/20240104-homeserver-nginx-12.png)

![hello server 1 : 서버 1로 요청을 보냈다.](/blog/images/20240104-homeserver-nginx-13.png)

아쉬운 점은 홈서버에서 서버1과 서버2를 각각 만들어서, 내용수정시 각각 다 해주고 있는데 이건 좀 비효율적인 방식 같다… 한번에 수정할 수 있는 방법은 없을까??? 실무에서는 어떻게 진행하는 것일까?
